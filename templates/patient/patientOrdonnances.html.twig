{% extends 'base.html.twig' %}

{% block title %}Mes Ordonnances{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/medecin/dashboard.css') }}">
{% endblock %}

{% block body %}
<div class="portal">

    <!-- SIDEBAR -->
    {% include 'side_bar.html.twig' %}


    <!-- MAIN CONTENT -->
    <main class="portal__main">

        <div class="portal__top">
            <div>
                <h1 class="portal__title">Mes Ordonnances</h1>
                <p class="portal__subtitle">Ordonnances prescrites par votre médecin — suivi des médicaments.</p>
            </div>
        </div>

        <div class="portal__content">

            <div class="portal__card">
                {% if ordonnances is empty %}
                    <p>Vous n'avez pas encore reçu d'ordonnance.</p>
                {% else %}
                    <div class="list">
                        {% for o in ordonnances %}
                            <div class="rowItem">
                                <div>
                                    <div class="rowItem__title">{{ o.medicament }} — {{ o.dosage }}</div>
                                    <div class="rowItem__meta">
                                        Durée: {{ o.duree }} | Créée le: {{ o.createdAt ? o.createdAt|date('d/m/Y H:i') : '—' }}
                                    </div>
                                </div>
                                <div style="display:flex;gap:8px;align-items:center;flex-wrap:wrap">
                                    <span style="background:rgba(19,184,194,.15);color:#13B8C2;padding:6px 12px;border-radius:8px;font-size:12px;font-weight:900">💊 Ordonnance</span>
                                    <a href="{{ path('Ordonnance_show', {'id': o.id}) }}" class="btn btn--ghost">Détails</a>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

        </div>

    </main>

</div>

{% endblock %}
