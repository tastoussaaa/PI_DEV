{% extends 'base.html.twig' %}

{% block title %}{{ produit.nom }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/medecin/dashboard.css') }}">
{% endblock %}

{% block body %}
{% set navigation = [
    {'name': 'Dashboard', 'path': path('medecin_dashboard'), 'icon': '🏠'},
    {'name': 'Consultations', 'path': path('medecin_consultations'), 'icon': '🩺'},
    {'name': 'Formations', 'path': path('medecin_formations'), 'icon': '📚'},
    {'name': 'Produits', 'path': path('produit_add'), 'icon': '📦'}
] %}

<div class="portal">
    {% include 'side_bar.html.twig' %}
    <main class="portal__main">
        <div class="portal__top">
            <div>
                <h1 class="portal__title">{{ produit.nom }}</h1>
                <p class="portal__subtitle">{{ produit.categorie }}</p>
            </div>
            <a href="{{ path('produit_edit', { id: produit.id }) }}" class="btn btn--primary">Modifier</a>
        </div>
        <div class="box" style="max-width:600px">
            {% if produit.imageName %}
                <p style="margin-bottom:16px;"><img src="{{ asset('uploads/produits/' ~ produit.imageName) }}" alt="{{ produit.nom }}" style="max-width:200px; height:auto;"></p>
            {% endif %}
            <p><strong>Nom :</strong> {{ produit.nom }}</p>
            <p><strong>Catégorie :</strong> {{ produit.categorie }}</p>
            <p><strong>Description :</strong> {{ produit.description }}</p>
            <p><strong>Prix :</strong> {{ produit.prix|number_format(2, ',', ' ') }} €</p>
            <p><strong>Stock :</strong> {{ produit.stock }}</p>
            <a href="{{ path('produit_add') }}" class="btn btn--ghost" style="margin-top:16px;">Retour à la liste</a>
        </div>
    </main>
</div>
{% endblock %}
